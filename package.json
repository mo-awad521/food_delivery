{
  "name": "food_delivery",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "dev:api": "pnpm --filter api run start:dev",
  "dev:mobile": "pnpm --filter mobile start --dev-client",
  "build:mobile": "pnpm --filter mobile exec expo run:android",
  "dev": "concurrently --raw \"pnpm dev:api\" \"pnpm dev:mobile\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.0",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
